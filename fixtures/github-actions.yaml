apiVersion: configs.flanksource.com/v1
kind: Config
metadata:
  name: github-actions-scraper
spec:
  githubActions:
    - owner: flanksource
      repository: config-db
      personalAccessToken:
        value: <pat>
