input:
  config_access:
    - id: canonical
      external_config_id:
        config_type: Org
        external_id: org-1
      external_user_aliases: [alice]
  access:
    - id: alias
      external_config_id:
        config_type: Org
        external_id: org-1
      external_user_aliases: [bob]

expected:
  config_access:
    - id: canonical
