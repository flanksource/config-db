apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: file-external-users-scraper
spec:
  full: true
  file:
    - type: Organization
      class: Organization
      id: $.id
      paths:
        - fixtures/data/external_users_test.json
