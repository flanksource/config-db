apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: file-config-access-scraper
spec:
  full: true
  file:
    - type: Organization
      class: Organization
      id: $.id
      paths:
        - fixtures/data/config_access_test.json
