apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: file-config-access-roles-scraper
spec:
  full: true
  file:
    - type: Organization
      class: Organization
      id: $.id
      paths:
        - fixtures/data/config_access_roles_test.json
