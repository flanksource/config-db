apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: file-permission-changes-scraper
spec:
  full: true
  file:
    - type: Organization
      class: Organization
      id: $.id
      paths:
        - fixtures/data/permission_change_test.json
