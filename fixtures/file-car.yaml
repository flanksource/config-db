apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: file-car-scraper
spec:
  file:
    - type: Car
      class: Car
      id: $.reg_no
      paths:
        - fixtures/data/car.json
