apiVersion: configs.flanksource.com/v1
kind: Config
metadata:
  name: git-postgres-propertes-scraper
spec:
  file:
    - format: properties
      type: PostgreSQLProperties
      id: postgresql-properties
      paths:
        - ./fixtures/file-postgres-properties.conf
