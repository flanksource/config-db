input:
  config_access:
    - id: a1
      user: alice
    - id: a1
      config_id: abc
  external_users:
    - name: alice
      aliases: [alice]
pre_populate:
  users:
    alice: "00000000-0000-0000-0000-000000000001"
assertions:
  - size(config_access) == 0
  - summary.config_access.skipped == 2
  - size(warnings) == 2
  - warnings[0].error == 'config_access missing config reference'
  - warnings[1].error == 'config_access missing user/group reference'
