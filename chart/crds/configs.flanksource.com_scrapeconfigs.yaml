---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: scrapeconfigs.configs.flanksource.com
spec:
  group: configs.flanksource.com
  names:
    kind: ScrapeConfig
    listKind: ScrapeConfigList
    plural: scrapeconfigs
    singular: scrapeconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ScrapeConfig is the Schema for the scrapeconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ScraperSpec defines the desired state of Config scraper
            properties:
              aws:
                items:
                  description: AWS ...
                  properties:
                    accessKey:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    assumeRole:
                      type: string
                    cloudtrail:
                      properties:
                        exclude:
                          items:
                            type: string
                          type: array
                        max_age:
                          type: string
                      type: object
                    compliance:
                      type: boolean
                    connection:
                      description: ConnectionName of the connection. It'll be used
                        to populate the endpoint, accessKey and secretKey.
                      type: string
                    cost_reporting:
                      properties:
                        database:
                          type: string
                        region:
                          type: string
                        s3_bucket_path:
                          type: string
                        table:
                          type: string
                      type: object
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    endpoint:
                      type: string
                    exclude:
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    include:
                      items:
                        type: string
                      type: array
                    inventory:
                      type: boolean
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    patch_details:
                      type: boolean
                    patch_states:
                      type: boolean
                    region:
                      items:
                        type: string
                      type: array
                    secretKey:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    skipTLSVerify:
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    trusted_advisor_check:
                      type: boolean
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                  required:
                  - region
                  type: object
                type: array
              azure:
                items:
                  properties:
                    clientID:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    clientSecret:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    connection:
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    organisation:
                      type: string
                    subscriptionID:
                      type: string
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    tenantID:
                      type: string
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                  required:
                  - organisation
                  - subscriptionID
                  - tenantID
                  type: object
                type: array
              azureDevops:
                items:
                  properties:
                    connection:
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    organization:
                      type: string
                    personalAccessToken:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    pipelines:
                      items:
                        type: string
                      type: array
                    projects:
                      items:
                        type: string
                      type: array
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                  required:
                  - pipelines
                  - projects
                  type: object
                type: array
              file:
                items:
                  description: File ...
                  properties:
                    connection:
                      description: ConnectionName is used to populate the URL
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    icon:
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    ignore:
                      items:
                        type: string
                      type: array
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    paths:
                      items:
                        type: string
                      type: array
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                    url:
                      type: string
                  type: object
                type: array
              full:
                description: Full flag when set will try to extract out changes from
                  the scraped config.
                type: boolean
              githubActions:
                items:
                  properties:
                    connection:
                      description: ConnectionName, if provided, will be used to populate
                        personalAccessToken
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    owner:
                      type: string
                    personalAccessToken:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    repository:
                      type: string
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                    workflows:
                      items:
                        type: string
                      type: array
                  required:
                  - owner
                  - personalAccessToken
                  - repository
                  - workflows
                  type: object
                type: array
              kubernetes:
                items:
                  properties:
                    allowIncomplete:
                      type: boolean
                    clusterName:
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    event:
                      properties:
                        exclusions:
                          properties:
                            name:
                              items:
                                type: string
                              type: array
                            namespace:
                              items:
                                type: string
                              type: array
                            reason:
                              items:
                                type: string
                              type: array
                          required:
                          - name
                          - namespace
                          - reason
                          type: object
                        severityKeywords:
                          description: SeverityKeywords is used to identify the severity
                            from the Kubernetes Event reason.
                          properties:
                            error:
                              items:
                                type: string
                              type: array
                            warn:
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    exclusions:
                      properties:
                        kind:
                          items:
                            type: string
                          type: array
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        name:
                          items:
                            type: string
                          type: array
                        namespace:
                          items:
                            type: string
                          type: array
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                    fieldSelector:
                      type: string
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    kubeconfig:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                        valueFrom:
                          properties:
                            configMapKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                            secretKeyRef:
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              type: object
                          type: object
                      type: object
                    maxInflight:
                      format: int64
                      type: integer
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    namespace:
                      type: string
                    scope:
                      type: string
                    selector:
                      type: string
                    since:
                      type: string
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                    useCache:
                      type: boolean
                  type: object
                type: array
              kubernetesFile:
                items:
                  properties:
                    container:
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    files:
                      items:
                        properties:
                          format:
                            type: string
                          path:
                            items:
                              type: string
                            type: array
                        type: object
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    selector:
                      properties:
                        fieldSelector:
                          type: string
                        kind:
                          type: string
                        labelSelector:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                  required:
                  - selector
                  type: object
                type: array
              logLevel:
                type: string
              schedule:
                type: string
              sql:
                items:
                  properties:
                    auth:
                      description: Authentication ...
                      properties:
                        password:
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                            valueFrom:
                              properties:
                                configMapKeyRef:
                                  properties:
                                    key:
                                      type: string
                                    name:
                                      type: string
                                  required:
                                  - key
                                  type: object
                                secretKeyRef:
                                  properties:
                                    key:
                                      type: string
                                    name:
                                      type: string
                                  required:
                                  - key
                                  type: object
                              type: object
                          type: object
                        username:
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                            valueFrom:
                              properties:
                                configMapKeyRef:
                                  properties:
                                    key:
                                      type: string
                                    name:
                                      type: string
                                  required:
                                  - key
                                  type: object
                                secretKeyRef:
                                  properties:
                                    key:
                                      type: string
                                    name:
                                      type: string
                                  required:
                                  - key
                                  type: object
                              type: object
                          type: object
                      required:
                      - password
                      - username
                      type: object
                    connection:
                      description: Connection is either the name of the connection
                        to lookup or the connection string itself.
                      type: string
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    driver:
                      type: string
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    query:
                      type: string
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                  required:
                  - connection
                  - query
                  type: object
                type: array
              trivy:
                items:
                  properties:
                    compliance:
                      items:
                        type: string
                      type: array
                    createFields:
                      description: CreateFields is a list of JSONPath expression used
                        to identify the created time of the config. If multiple fields
                        are specified, the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    deleteFields:
                      description: DeleteFields is a JSONPath expression used to identify
                        the deleted time of the config. If multiple fields are specified,
                        the first non-empty value will be used.
                      items:
                        type: string
                      type: array
                    format:
                      description: Format of config item, defaults to JSON, available
                        options are JSON, properties
                      type: string
                    id:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    ignoreUnfixed:
                      type: boolean
                    ignoredLicenses:
                      items:
                        type: string
                      type: array
                    items:
                      description: A JSONPath expression to use to extract individual
                        items from the resource, items are extracted first and then
                        the ID,Name,Type and transformations are applied for each
                        item.
                      type: string
                    kubernetes:
                      description: TrivyK8sOptions holds in Trivy flags that are Kubernetes
                        specific.
                      properties:
                        components:
                          items:
                            type: string
                          type: array
                        context:
                          type: string
                        kubeconfig:
                          type: string
                        namespace:
                          type: string
                      type: object
                    licenseFull:
                      type: boolean
                    name:
                      description: A static value or JSONPath expression to use as
                        the ID for the resource.
                      type: string
                    scanners:
                      items:
                        type: string
                      type: array
                    severity:
                      items:
                        type: string
                      type: array
                    tags:
                      additionalProperties:
                        type: string
                      description: JSONStringMap defiend JSON data type, need to implements
                        driver.Valuer, sql.Scanner interface
                      type: object
                    timeout:
                      type: string
                    timestampFormat:
                      description: TimestampFormat is a Go time format string used
                        to parse timestamps in createFields and DeletedFields. If
                        not specified, the default is RFC3339.
                      type: string
                    transform:
                      properties:
                        exclude:
                          description: Fields to remove from the config, useful for
                            removing sensitive data and fields that change often without
                            a material impact i.e. Last Scraped Time
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        expr:
                          type: string
                        gotemplate:
                          type: string
                        include:
                          items:
                            properties:
                              jsonpath:
                                type: string
                            type: object
                          type: array
                        javascript:
                          type: string
                        jsonpath:
                          type: string
                        mask:
                          description: Masks consist of configurations to replace
                            sensitive fields with hash functions or static string.
                          items:
                            properties:
                              jsonpath:
                                type: string
                              selector:
                                properties:
                                  type:
                                    description: Type is the config type to apply
                                      the mask
                                    type: string
                                type: object
                              value:
                                description: Value can be a hash function name or
                                  just a string
                                type: string
                            type: object
                          type: array
                      type: object
                    type:
                      description: A static value or JSONPath expression to use as
                        the type for the resource.
                      type: string
                    version:
                      description: Common Trivy Flags ...
                      type: string
                    vulnType:
                      items:
                        type: string
                      type: array
                  type: object
                type: array
            type: object
          status:
            description: ScrapeConfigStatus defines the observed state of ScrapeConfig
            properties:
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
